name: Build Android AAB

jobs:
  build_android:
    name: Android production build
    type: build
    params:
      platform: android
      profile: production
    hooks:
      before_install_node_modules:
        - name: Restore frozen workspace source
          run: node ../../scripts/restore-pnpm-lock.cjs